import React from 'react';
import {ActivityIndicator} from "react-native";

export const Loading = ({}) => (
  <ActivityIndicator size="large" color="#9abdb5" style={{paddingBottom: 20, paddingTop: 30}} />
);




